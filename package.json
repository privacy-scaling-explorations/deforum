{
  "name": "pse-forum",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "docker-compose up --build --watch",
    "build": "turbo run build",
    "clean": "turbo run clean",
    "generate": "yarn workspace client generate"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "@types/node": "^22.14.1",
    "dotenv": "^16.5.0",
    "jsonwebtoken": "^9.0.2",
    "turbo": "^1.13.3",
    "typescript": "^5.4.5",
    "vite-tsconfig-paths": "^5.1.4"
  },
  "packageManager": "yarn@1.22.22",
  "dependencies": {
    "@trpc/server": "^11.0.0"
  }
}
