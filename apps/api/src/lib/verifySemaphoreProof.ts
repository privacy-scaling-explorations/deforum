import { Identity } from '@semaphore-protocol/identity';

export const verifySemaphoreProof = async (
  proof: string,
  nullifier: string,
  publicSignals: string[]
) => {
  console.warn('DUMMY SEMAPHORE PROOF VERIFICATION');
  return true;
};
